# 08 · Document assistant

**Not built yet.** `core.documents` and `quality.rag_assistant` are ANNOUNCED, which means
_disabled_ for authorization: their routes answer **404**, exactly like a URL that means nothing.
The rail shows them as non-navigable placeholders.

This page is filled in by Slice 6.

# 09 · Report generation

**Not built yet.** `reports.social_generator` is ANNOUNCED, therefore disabled:
`/t/…/p/…/reports` answers **404**, and the page never names the capability key or who could
enable it.

This page is filled in by Slice 7.

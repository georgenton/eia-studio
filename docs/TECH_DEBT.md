# Intentional technical debt

Each entry has an owner and a removal trigger (CLAUDE.md rule 13). Debt without a trigger is not
accepted.

| Id | Debt | Why now | Owner | Removal trigger |
|---|---|---|---|---|
| TD-001 | Local/CI PostgreSQL image is built from `docker/postgres/Dockerfile` on every fresh CI runner (~1–2 min, then cached per runner). The base is now pinned by manifest-list digest (IG0-M01), but the `postgresql-17-pgvector` apt package is resolved at build time, so the built layer is not bit-reproducible. | No maintained upstream image ships both PostGIS and pgvector; publishing our own image needs a registry decision. | dev | Push a built image to GHCR from a workflow and pull it by digest in Testcontainers/compose, when CI time matters or a second contributor joins; that also pins pgvector. |
| TD-002 | Tenant entitlements are rows in `app.tenant_capability` written at tenant creation from `DEFAULT_PLAN_ENTITLEMENTS` (the catalogue's non-extension keys); there is no `plan` / `plan_entitlement` table yet. | No billing or plan concept exists in Slice 0. | dev | Billing/plan slice: introduce `plan` + `plan_entitlement`, derive `entitled` from the plan, keep the column as a materialised cache or drop it. |
| ~~TD-003~~ | ~~The domain package depends on `@eia/db`.~~ **Resolved at Implementation Gate 0 (IG0-B02).** Orchestration moved to `packages/application`; `packages/domain` is pure and the boundary is enforced by ESLint plus `packages/domain/test/purity.test.ts` (ADR-015). | — | — | Closed 2026-09-01. |
| TD-004 | Project capability rows only restrict; the database has no trigger preventing an `enabled = true` override row for a key the tenant lacks (the resolver's AND and the write-time guard cover it). | Trigger logic would duplicate the resolver in SQL. | dev | If a second writer (import, admin tool) can create override rows, add a constraint trigger mirroring `assertProjectOverrideAllowed`. |
| TD-005 | The Client Portal's external half is not built: no `ClientPortalGrant` row, no portal cookie, no `PortalContext`, and **`eia_portal` is granted nothing**. The `portal` schema and `portal.client_publication` now exist and are read by the ordinary authenticated internal path (ADR-027). | The publication wave built the projection and the surfaces, deliberately without external authentication: a second authentication surface is a security surface of its own, and half of one is worse than none. A role granted SELECT so it looks implemented is surface with nobody behind it, so it stays unable to read. | dev | The first real external client. Then: `ClientPortalGrant`, a portal session and `PortalContext` that cannot be handed to an internal use-case, `GRANT SELECT ON portal.client_publication TO eia_portal` under its own policy, and the isolation suite that proves a portal session reaches no internal route. `loadPublishedClientView` keeps its shape and gains a second caller. |
| TD-006 | Foundation UI (sign-in, tenant/project context pages) uses a CSS module and inline forms; no `packages/ui` components exist yet. | Slice 0 forbids product screens; components arrive with Portfolio/shell. | dev | Portfolio slice: replace foundation pages with the shell and `packages/ui` primitives; keep `/health` and the capability probe route. |
| TD-007 | Better Auth runs with the runtime role's CRUD grants on the `auth` schema (no RLS there; tables are not tenant-scoped). Session lookups therefore trust Better Auth's own row filtering. | Identity tables have no tenant column by design (ADR-010). | dev | Review at the compliance gate; consider a dedicated `eia_auth` role if the auditor requires separation between identity and operational data access. |
| TD-008 | Membership removal and last-owner protection on removal are not implemented (only role change is). | Tenant Settings › Usuarios arrives with the Portfolio/shell slice. | dev | Implement `removeTenantMembership` with the last-owner rule, cascade semantics and audit when that surface is built. |
| TD-009 | `eia_readonly` (BI) role from ADR-004 is not created. | No consumer. | dev | First analytics/BI requirement. |
| TD-010 | **Deferred onboarding work, no longer an exposed failure mode.** Public self-signup is disabled (IG0-H01), so the non-transactional Better Auth sign-up path is unreachable by anonymous callers. Creating an identity together with the application user, `TenantMembership` and optional `ProjectMembership` still has no atomic or compensating workflow; identities are provisioned deliberately meanwhile. | Signup is closed; the gap is now in future onboarding, not in a live endpoint. | dev | Onboarding slice: design provisioning (invitation → identity → app user → membership) with a compensating or transactional path and reconciliation for partial failures, then decide whether to re-open self-signup. |
| TD-011 | In development the Better Auth instance is recreated per request (`NODE_ENV=development`) so schema hot reloads are picked up; production caches it on `globalThis`. | Dev ergonomics after the `issuer` schema change. | dev | Remove when the auth schema is stable (after Slice 0 review) if the per-request cost is noticeable. |
| TD-012 | The Postgres host port for docker-compose is `EIA_PG_PORT` (default 5433) because 5432/5433 were occupied on the developer machine; CI uses Testcontainers on random ports. | Machine-specific conflict. | dev | None required; documented in README and `.env.example`. |
| TD-013 | Migration drift check (`db:check`) runs `drizzle-kit generate` and cleans up; a native `drizzle-kit check`-style drift command would be lighter. | drizzle-kit 0.31 lacks a no-write drift command for this setup. | dev | When drizzle-kit ships a dry-run diff. |
| TD-014 | Branch protection / rulesets for `main` cannot be configured: the repository is private on a GitHub Free personal plan (API returns 403 "Upgrade to GitHub Pro or make this repository public"). Squash-only merge and delete-branch-on-merge are set; PR-required, required checks, linear history and force-push blocking rely on developer discipline and CI until then. | GitHub plan limitation; the repository must stay private. | dev | Upgrade to GitHub Pro (or move the repository to an organisation plan), then apply the ruleset in DEPLOYMENT.md §7 with contexts `quality` and `db`. |
| TD-015 | Project profile defaults are resolved from the in-code system-profile registry via the project's `profile_key`; an unknown key contributes no defaults (falls through to enabled, still under the tenant ceiling). Profile *versions* are recorded but not yet used to select a historical default set. | One system profile exists; versioned profile snapshots arrive with the project-profile slice. | dev | Project-profile slice: persist the profile snapshot (or a versioned registry) and resolve defaults by `profile_key` + `profile_version`. |
| TD-016 | The staging database presents a self-signed TLS certificate generated by `docker/postgres/ssl-entrypoint.sh`, so clients connect with `sslmode=no-verify`: encrypted in transit, server certificate not pinned to a CA. Scope is now bounded in code: `APP_ENV=production` requires `sslmode=verify-full` or `verify-ca` on `DATABASE_URL` and `DATABASE_MIGRATOR_URL`, so this mode cannot reach production (SG-005). | Upstream PostGIS images ship without TLS and staging holds only synthetic data. | dev | Before production: use a provider-issued certificate (managed PostgreSQL) or an internal CA, and move clients to `sslmode=verify-full`. The guard already fails the boot if that is not done. |
| ~~TD-017~~ | ~~`railway.toml` (Config as Code) is deprecated by Railway.~~ **Resolved at Staging Gate 0.5 (SG-002).** `railway.toml` was deleted and the staging environment is described by `.railway/railway.ts`, applied with `railway config plan` / `railway config apply` (docs/DEPLOYMENT.md §3.4). | — | — | Closed 2026-09-02. Residual limitations are tracked separately as TD-021. |
| TD-018 | The staging database is a self-built container, so we own base-image patching and major-version upgrades, and it has **no backup and no point-in-time recovery**. The two gaps have different causes and must not be conflated. *Not available:* scheduled and on-demand volume backups are refused for this Railway workspace — `railway postgres pitr schedule set --daily` returns "You do not have access to this resource" and the `volumeInstanceBackupScheduleUpdate` / `volumeInstanceBackupCreate` mutations return "Not Authorized"; enabling one was attempted and failed, so nothing is configured. *Not applicable:* PITR runs only inside Railway's own database images (`postgres-ssl`, `postgres-patroni`, per `railway postgres pitr status`), and ours is a PostGIS build. | Railway's managed template lacks PostGIS; staging is disposable, synthetic and never a source of truth. | dev | Production gate: evaluate managed PostgreSQL with PostGIS + pgvector + PITR (Neon, Supabase, Crunchy Bridge, RDS), decide the required recovery guarantee (frequency, retention, restore procedure, verified restore) and record it as an ADR-012 amendment. Production may not inherit staging's posture. |
| TD-019 | `vercel deploy` from the repository root exceeds Vercel's 100 MB single-file upload limit, so CLI deploys of the whole workspace fail. Previews come from the GitHub integration instead, which is the intended path anyway. | The local upload includes build artefacts the Git integration never sees. | dev | Only if a CLI deploy is ever needed: add a `.vercelignore` covering build output and large fixtures. |
| TD-021 | `railway config plan` never reaches a clean state: it permanently reports one pending change, `postgres-gis source.type` `github → empty`. Railway keeps returning the historical source type for a service with no repository connected, so applying it succeeds and changes nothing. `restartPolicyType` behaves the same way and is therefore not declared (the platform default is already `ON_FAILURE`). Volume backup schedules cannot be expressed at all: the graph compiler discards the field. | Railway CLI 5.47.2 / SDK `railway@3.11.0` limitations, not configuration errors on our side. | dev | Re-test on a later Railway CLI/SDK; if the fields round-trip, declare `restartPolicyType` and the backup schedule in `.railway/railway.ts` and adopt `railway config plan --detailed-exit-code` as a CI drift gate. |
| ~~TD-020~~ | ~~Orphaned Railway volume `postgres-volume` from the retired stock PostgreSQL comparison service.~~ **Resolved at the start of Slice 1.** Verified detached, verified not the `postgres-gis` volume, verified unreferenced by `.railway/railway.ts` and by every staging variable, then deleted; `postgres-gis` and the worker stayed online and the database kept its data. | — | — | Closed 2026-09-02. |
| TD-022 | **Narrowed at Implementation Gate 1 (IG1-005).** An automated axe smoke suite now runs in CI over sign-in, Portfolio, Command Center and the open provenance drawer, failing on `serious` and `critical` violations; it found and forced the fix of a real contrast defect in the label palette. What remains open is the part a scanner cannot do: no assistive-technology walkthrough has been performed, and axe covers only a minority of WCAG criteria, so a green run is not evidence of conformance. | A screen-reader pass needs a human with the tooling and is worth doing over more than one surface. | dev | When GIS and Social land: one assistive-technology pass over the shell, the switchers, the drawer and a data grid, recorded with what was found. |
| TD-023 | **Narrowed in Slice 2.** The territorial summary is now built, counted from the active GIS layers and carrying their provenance. Two panels of the approved composition remain omitted: project instruments and quality findings. A panel on the screen says so explicitly rather than leaving silent gaps. | Populating them would mean fabricating field or Quality Gate metrics, which invariant 4 forbids. | dev | Restore each remaining panel with its own slice: instruments with Field Surveys, findings with Quality Gate. |
| ~~TD-024~~ | ~~`@eia/ui` imports `next/link` and `next/navigation`.~~ **Resolved at Implementation Gate 1 (IG1-004).** Components that construct routes or drive the router — the rail, the breadcrumb, the switchers, the provenance link, the button link — moved to `apps/web/components/navigation`; the drawer now takes an `onClose` callback and a thin application wrapper binds it to the router. `@eia/ui` declares no framework in any dependency field, and `packages/ui/test/framework-agnostic.test.ts` fails if a router import returns. No adapter or navigation port was invented. | — | — | Closed 2026-09-02. |
| TD-025 | The approved palette's “texto tenue” (`#7A858E`) fails WCAG AA on white at the 9,5 px label size the design uses it for (3,77:1). The token was darkened to `#6B747C` (4,76:1 on white, 4,59:1 on the subtle surface) and the highlighted forecast cell now reads in the technical blue. The design bundle itself is unchanged and still specifies the failing value. | Correcting the token was the smallest fix that keeps the ink → secondary → muted hierarchy; changing the approved bundle is not this repository's decision. | dev | Raise the contrast finding with the design team for v0.3 so the bundle and the implementation agree; until then the token comment and DESIGN_BUNDLE_KNOWN_ISSUES record why they differ. |
| TD-026 | MapLibre's GeoJSON worker is served from `apps/web/public/vendor/maplibre/`, copied there by `tooling/scripts/copy-maplibre-worker.mjs` before `dev` and `build`. Left to the bundler, the worker either starts from a blob that never completes its handshake or is emitted content-hashed without the sibling module it imports by relative path; either way every source stays unloaded and the map paints nothing, silently — no error event, no failed request. | The failure is silent and total, and no supported bundler configuration produced a working worker. Copying two files is the smallest thing that does. | dev | Re-test on each MapLibre or Next major upgrade; drop the copy step and the `setWorkerUrl` call as soon as the bundled worker starts on its own. |
| TD-027 | Parcel filtering and sorting happen in the browser over the whole project payload (`PARCEL_PAYLOAD_LIMIT = 2000`), and the map consumes GeoJSON rather than vector tiles. No table virtualization. | Measured for the pilot (Gate 2, IG2-008): 141 features, 46,7 KiB of GeoJSON, 117,2 KiB of read model, read models at 6,4 ms / 3,7 ms median, table visible ~535 ms and canvas sized ~800 ms in the browser. A server-side query API, MVT tiles and virtualization would be three subsystems solving a problem no project has. | dev | **Observational, not architectural**: a project whose payload approaches the 2 000-feature cap (≈650 KiB of GeoJSON at this density), or a measured interaction delay in the table. Then, in order and each with a measurement first: `ST_AsMVT` tiles (ARCHITECTURE.md §7), server-side filtering, TanStack Virtual. |
| TD-028 | The pilot's **analysis** CRS (`EPSG:32717`, where its lengths and areas are measured) is a demo assumption: the official GIS package has not been received, so the project's declared CRS is unknown. Canonical storage is `EPSG:4326` and is not affected. | A projected CRS is required to compute an area at all, and UTM 17S is defensible for the region. It is recorded as fixture data with `crsBasis: "DEMO_ASSUMPTION"`, never as a schema or domain constant (ADR-017). | dev | The first official GIS package: it declares its own CRS, the import records it as `source_srid`, transforms to canonical and supersedes the generated version (`docs/GIS_IMPORT_CONTRACT.md`). |
| TD-029 | The map has no base map. The approved design shows hydrography, towns and general location; the legend lists only the two layers we actually have. | Drawing a decorative background and labelling it REAL BASE MAP would be false (invariant 4), and a third-party tile provider is an unmade decision with licensing and privacy consequences. | dev | A decision on the base-map provider, or an official base layer in the GIS package; the legend key `REAL_BASE_MAP` already exists for it. |
| ~~TD-030~~ | ~~GIS geometry columns were typed `geometry(...,32717)`, making one pilot's UTM zone a platform-level constraint: a project outside zone 17S could not be stored.~~ **Resolved at Implementation Gate 2 (IG2-001)** by forward migration 0011. Canonical storage is now `EPSG:4326`; `source_srid` and `analysis_srid` are per-dataset metadata; `analysis_srid` must be projected; the pilot's zone lives only in its fixture, and a unit test fails if it reappears as a domain constant (ADR-017). | — | — | Closed 2026-09-02. |
| ~~TD-031~~ | ~~The sign-in form declared no `method`, so a submit before hydration fell back to a native GET and put the password in the URL, in history and in the `Referer` of subsequent requests.~~ **Resolved at Implementation Gate 2 (IG2-006).** `method="post"` closes the fallback, and `e2e/anonymous.spec.ts` asserts no credential reaches any URL, address bar, `window.location` or navigation entry, that no credential form is served without scripts, and that the identity endpoint refuses credentials as a query string. | — | — | Closed 2026-09-02. |
| TD-033 | The analysis-CRS contract is **projected + metre-based**: a projected CRS whose linear unit is the foot (EPSG:2225, EPSG:2263) is refused rather than converted. | The stored columns are metres and square metres; supporting another unit means carrying it alongside every measurement and converting at every boundary, for no project that exists. | dev | A real project that must be analysed in a non-metric projected CRS. Then the unit travels with the measurement, and `app.srid_is_metric_projected` becomes a unit lookup rather than a metre test. |
| ~~TD-034~~ | ~~`analysis_srid` was validated by numeric range (`NOT BETWEEN 4000 AND 4999`), which rejects `EPSG:4087` (projected, metres) and accepts `EPSG:6318` (geographic, degrees).~~ **Resolved at Implementation Gate 2 (IG2-009)** by forward migration 0012: validation reads the CRS definition from `spatial_ref_sys` — registered, `PROJCS`/`PROJCRS`, metre linear unit, and usable by `ST_Transform` — in `app.srid_is_metric_projected` and in `assertAnalysisSridUsable`. No numeric range, no curated code list, no authority assumption. | — | — | Closed 2026-09-02. |
| TD-032 | The affectation ≤ parcel-area invariant is a `CONSTRAINT TRIGGER` (`affectation_within_parcel`), not a CHECK, because the two areas live in different tables. | A CHECK cannot span tables, and the alternative — denormalising the parcel area onto every affectation — creates the duplicated fact this slice removed everywhere else. | dev | If affectation write volume ever makes the per-row trigger measurable, revisit with a deferred constraint over a materialised parcel-area column, and only with a measurement. |
| TD-035 | FieldFlow has no offline capture. The native web channel posts to the server, and a project whose `field.surveys.offline_mode` is `required` cannot activate a campaign at all. | Offline sync is conflict resolution, device identity, queue durability, partial submission and clock skew — a slice of its own. Shipping half of it behind a switch would read as a promise the product could not keep (ADR-018). | dev | A project that genuinely needs offline capture. Then either a capture-channel adapter under `docs/FIELD_CAPTURE_ADAPTER_CONTRACT.md`, or a first-party offline client; either way `supportsOffline` becomes true only when a survey can be completed with no connectivity and demonstrably arrive later. |
| ~~TD-036~~ | ~~A submitted response cannot be corrected.~~ **Resolved by ADR-038.** A correction is a **new response on a new assignment**, in the same campaign and against the same questionnaire, with `survey_correction` saying which response it replaces and why. The submitted row and its answers are as immutable as they were; what changed is that *which response the study means* is now a question with an answer. The entry predicted `SurveyInstanceCorrection` with actor, reason, the original preserved and provenance on both — which is what shipped. | — | — | Closed 18 Sep 2026 (`docs/SURVEY_CORRECTIONS.md`). |
| ~~TD-037~~ | ~~Field media is not captured.~~ **Resolved in Production V1 Wave 2 (ADR-032).** `field_media` exists with FORCE RLS under the row-ownership predicate of SECURITY.md §10b, written once by grant and trigger; EIA Field captures with the camera and uploads through the object-storage path of ADR-031; the Parcel Workspace's *Fotografías* tab lists what was taken. Photographs, not sketches or audio: the vocabulary is four kinds and deliberately holds no `document` or `signature`. | — | — | Closed 2026-09-17. |
| TD-038 | `SurveyInstance.respondent_user_id` is the **technician** who captured the response, not an identified respondent. There is no `pii.Respondent`, no consent record and no personal data in a survey answer. | No real personal data may be ingested before the compliance gate (SECURITY.md §10a), and the demo questionnaire deliberately asks nothing personal. | dev | The compliance gate, followed by the PII slice: `pii.Respondent` with pseudonymous ids, consent/legal-basis records, and the `pii.read` resource policy over the answers flagged PERSONAL or SENSITIVE. |
| TD-039 | Answer aggregation across survey versions is not possible: there is no `QuestionMapping (from_question_id → to_question_id, method)`. Each response is readable only against its own version. | Nothing aggregates yet, and a mapping invented before a second version exists would fix semantics nobody has needed. | dev | Social Intelligence, when a project has two published versions of one template and tabulation must span them (ADR-006). |
| TD-040 | Persistent-environment verification (`pnpm test:staging`) is an operator command, not a CI check: nothing automatically proves that staging still matches `main`'s schema between gates. | Running it in PR CI would put a shared environment's credentials in every pull request and make a green build depend on that environment's state — the coupling IG3-001 is about. | dev | A scheduled workflow against staging with a repository secret, once staging deploys are automatic (CI.md §2, Stage G); it must stay non-destructive and must not become a merge gate. |
| TD-041 | A submitted `HumanReview` cannot be corrected. There is no re-review, no supersede and no reopen — the database refuses every write to a submitted review and its categories. | The same argument as TD-036 one layer along: a correction that looks like the original decision is what a study cannot recover from, and the superseding workflow (actor, reason, both codings preserved) is a design question this slice did not need to answer. | dev | The first real re-coding requirement. Then a `HumanReviewSupersession` that points at the review it replaces, with the original preserved and provenance on both. | **Narrowed by ADR-038:** a coding of a *superseded* answer now stops contributing to current distributions on its own, because the analytics read the effective response. What remains is re-reviewing a coding of an answer that is still effective.
| TD-042 | The coding taxonomy is project-scoped. A firm cannot yet keep a reusable library of schemes across projects. | A tenant-level scheme needs its own RLS story, sharing rules and a decision about who may edit a scheme other projects are already coded against. Scoping it to the project keeps every existing tenancy guarantee and postpones nothing that is needed now. | dev | The second project that would code against the same scheme. Then a tenant-level taxonomy with project adoption, and a migration that keeps existing versions where they are. |
| TD-043 | There is no taxonomy authoring UI. Versions come from the fixture; no code path in the product writes a category. | Publishing a coding scheme is an editorial act with its own review, and the slice needed one published scheme, not an editor. | dev | When a consultancy supplies or wants to draft its own scheme. The immutability rules already define what an editor may and may not do. |
| TD-044 | No independent evaluation of the classifier. `HumanReview` is **not** a gold standard: the reviewer decided while looking at the proposal, so agreement measures concordance, not accuracy. | A gold standard requires blinded, independent coding by more than one expert and an adjudication procedure — a research protocol, not a product feature (ADR-019). | dev | The thesis evaluation slice. The data it needs (answer, taxonomy version, model, prompt hash, proposed labels, final labels, decision, latency, tokens, elapsed time) is already persisted; what is missing is the protocol and a blind coding mode whose server never returns an AI proposal. `docs/THESIS_ALIGNMENT.md` maps what is preserved for Conditions B and C and states the structural obstacle to Condition A: `human_review.classification_id` is NOT NULL, so a coding cannot exist without a proposal to attach to. |
| TD-045 | Social analytics require `field.responses.read`, even the aggregates. A VIEWER holding `social.read` is denied rather than shown a tabulation. | The counts are computed from response rows under RLS, so without that permission every figure would come back zero — a plausible-looking false tabulation. Denying is the honest failure. | dev | A published, project-level aggregate projection in the shape the client portal already uses, readable without seeing rows. Then `social.read` alone is enough for the Tabulación tab. |
| TD-046 | The worker and the web application share one database role (`eia_app_login`), so `app.claim_classification` is executable from a request path as well as from the worker. | A dedicated worker role means a second credential in every environment; the function is narrow by construction — it returns four uuids, flips one row to PROCESSING, and exposes no content — so the exposure is a claimed job, not data. | dev | When a second background workload exists, or before production: provision an `eia_worker_login` role, grant EXECUTE on the claim helpers to it alone, and revoke from `eia_app`. |
| TD-047 | Provider cost is not computed. Token counts and latency are stored per classification; money is not. | A hardcoded price table is wrong the day a provider changes it, and the platform does not return an authoritative cost figure for these calls. | dev | An evaluation that needs cost: multiply the stored usage by externally versioned pricing at analysis time, or store an authoritative figure if the gateway starts returning one. |
| TD-048 | Manual coding without an AI proposal is not supported: a response can only be reviewed once a classification has succeeded. | `HumanReview` references the proposal it reviewed, which is what makes the agreement comparison meaningful. Allowing a review with no proposal would either weaken that reference or need a second review shape. | dev | A project that codes without AI at all, or the blind-coding mode of TD-044 — both need a review that stands on its own, and that is the moment to model it. |
| TD-049 | Assisted coding on persistent staging is unavailable: `LIVE_AI = BLOCKED_EXTERNAL_CONFIG`. No `AI_GATEWAY_API_KEY` is configured for the web app or the worker, and IG4-001 forbids substituting the deterministic fake there. | The credential is an owner decision with a cost and a vendor-registry entry behind it (SECURITY.md §10a); running the fake instead would put proposals in `ai_classification` that nobody could distinguish from a model's. | owner | The owner supplies `AI_GATEWAY_API_KEY` to the Vercel project and the Railway worker and sets `SOCIAL_CLASSIFIER=ai-gateway` with a provider-qualified model. Nothing in the code changes; the surface stops reporting the blocked state on its own. |
| TD-050 | A quality rule cannot be changed without a deployment: the catalogue is code (ADR-020), not a table a tenant can edit. | The intended trade. A rule makes a claim about a study, and a definition editable in the database while its implementation lives in TypeScript is one rule with two homes that nothing keeps in agreement. | dev | A firm that genuinely needs its own rule. Then a `quality_requirement` table whose rows *select* a detector key from this registry — data choosing an implementation, never data being one. No schema written today prevents it. |
| TD-051 | The project's jurisdiction is parsed from its `location_label` (the penultimate comma-separated part), which `rule.territorial_institution` compares against. | A structured territory on `project` is a schema change several slices would have to agree on, and the pilot's label is well-formed. Parsing a label is a compromise, recorded rather than pretended away. | dev | The second project whose label is shaped differently, or the GIS import that brings an official administrative division. Then `project` carries province/canton/parish and the rule reads them. |
| TD-052 | A finding has no assignee. ADR-008 describes `assignee_membership_id` and a "take for review" that names a person; `START_REVIEW` moves the state and records who decided, but nothing is assigned. | Assignment needs a notion of workload, notifications and reassignment that no surface asks for yet, and the decision history already answers "who looked at this". | dev | A project with more than one reviewer, where "who is on it" stops being obvious. |
| TD-053 | Quality runs are manual only. `quality_run.trigger` admits `SCHEDULED` and `EVENT`; nothing produces either. | A scheduled run needs the worker, a schedule per project and a decision about what a nightly reopening does to a reviewer's queue. The reconciliation is already safe for it — that was the point of the fingerprint. | dev | When a project's documents change often enough that a manual run is missed. |
| TD-054 | A finding is never linked to a parcel. `quality_finding.parcel_id` exists and is always null, so the Parcel Workspace's Calidad tab stays inert. | None of the five pilot rules is about a single parcel: they compare document-level statements. A rule that fires per parcel is the natural first user of the column. | dev | The first per-parcel rule (a geometry or affectation check against an imported cadastre). Then the Calidad tab reads findings by `parcel_id`. |
| TD-055 | The "module not implemented" state (system state 15) has **no reachable route**. Every workspace surface whose capability can be effective is now built, and the rest are ANNOUNCED, therefore never effective, therefore 404. The browser-level test for the state was removed. | Manufacturing a route for it would mean shipping a capability nobody implements, purely so a test could visit it. The policy is still asserted where it is decided — a pure function over the surface registry — and the one place that maps it is `apps/web/lib/surface-access.ts`. | dev | Slice 6, when `core.documents` becomes AVAILABLE before the Documents surface exists: the state gets a real route again and the browser assertion comes back with it. |
| ~~TD-056~~ | ~~Document ingestion accepts extracted text only; no upload, no PDF parser, no OCR.~~ **Resolved in Production V1 Wave 2 (ADR-031, ADR-033).** Upload is real, and extraction reads a PDF with `pdf.js` (page locators) and a DOCX by opening the archive itself (heading-trail locators, and no invented page numbers). OCR is still not built and is now its own entry, TD-098 — a scanned PDF is `REQUIRES_OCR` and contributes no chunk, so it can never be cited. | — | — | Closed 2026-09-17. |
| TD-057 | Retrieval is lexical. A question phrased with different words than the document will find nothing, however clearly the document says it. | No embedding provider is configured anywhere (TD-049), and a vector column filled by a deterministic stand-in is indistinguishable from a real one — the failure IG4-001 exists to prevent, one slice later and in a column (ADR-021). The surface says which strategy answered, in words, so the limit is visible rather than implied. | owner → dev | An embedding credential. Then: a migration adding `vec` and the embedding column under the same RLS predicates, an `Embedder` port, a second `DocumentRetriever`, and a decision about combining the two. Nothing shipped in Slice 6 has to be undone. |
| TD-058 | The assistant answers one question at a time and keeps no conversation. There is no follow-up, no history and no thread. | A conversation needs a decision about what a follow-up is grounded in — the previous passages, a fresh retrieval, or both — and getting that wrong is how a citing assistant starts answering from the model's memory of the last turn instead of from the documents. | dev | When a specialist's real usage shows the single-question shape is the obstacle. |
| TD-059 | An assistant answer is never stored. Nothing records what was asked of a project's documents beyond one audit line, and a useful answer cannot be kept beside a finding or a report section. | Storing generated prose as project content requires a provenance record marking it `DERIVED` with `validation_status = pending` (AI_GOVERNANCE.md §8), and no surface in this slice would render such a record honestly. Slice 7's report snapshot is the first place that does. | dev | Slice 7, which needs exactly this: a stored, provenance-bearing derived text with a human decision attached. |
| TD-060 | A report version has one status, `DRAFT`. There is no approval workflow: `deliverables.approve` exists as a permission and nothing consumes it, and the .docx says "BORRADOR" on its face because nothing in the system can say otherwise. | Approving a deliverable is a decision with legal weight, and the workflow around it — who, on what basis, what happens to earlier versions, what a client may then see — is a design question this slice did not need to answer to make the chapter traceable. Shipping a button that said "approve" without that would be worse than shipping none. | dev | The first real delivery. Then a `ReportApproval` in the shape `SpecialistReview` already established: append-only, justified, attributed, and never editable — plus the portal question of what an approved version makes publishable. |
| TD-061 | The narrative grounding check compares digit sequences. It can tell that a paragraph states a number the section did not compute; it cannot tell whether the sentence around that number is true. | A semantic check would need a second model judging the first, which is a research question rather than a control, and the failure worth preventing here is the invented figure — the one that survives review because the sentences around it are right. | dev | An evaluation of generated narrative quality, which needs a live provider first (TD-049). |
| TD-062 | The chapter has one shape: five fixed sections of the social component. It is not configurable, and there is no second report kind. | A chapter's shape is an editorial decision, and a project needing a different one needs a different report kind rather than a settings page. `generated_report.kind` is the extension point and is already unique per project. | dev | The environmental chapter, or a consultancy whose social chapter genuinely differs. Then a second kind with its own snapshot builder; nothing about versioning, traceability or rendering changes. |
| TD-063 | The .docx is rendered on every download rather than stored. A large chapter re-renders each time. | It is a deterministic function of a stored, immutable snapshot, so a stored copy would be a second artefact that can drift from the one that is versioned. The pilot's chapter renders in milliseconds. | dev | A chapter large enough for the render to be felt. Then store it in object storage keyed by the version id — which is safe precisely because the version cannot change. |
| ~~TD-064~~ | ~~`buildRequestContext` opens four separate transactions, so 12 of the 17 round trips every project page spends identifying its caller are framing rather than questions.~~ **Resolved in the real-data wave.** One transaction now reconciles the user, proves the tenant with `app.tenant_id` unset, adopts it (`adoptTenantContext`) and reads the project, memberships and capability rows under it. Measured: context round trips 17 → 12 on every project route, 14 → 9 on the Portfolio, and 13 fewer round trips per page overall (`docs/PERFORMANCE_BASELINE.md` §10). The merged envelope is covered by `packages/application/test/request-context-envelope.integration.test.ts`. | — | — | Closed 2026-09-04. |
| TD-065 | Social Intelligence issues **78 database queries per page render** and spends 377 ms in the driver on a local database — nine times any other route. | Deterministic tabulation reads answers per question and per option, which is arithmetic expressed as many small queries. Every figure it produces is asserted by tests against declared denominators, so a rewrite must not move a single number, and doing that carefully is a piece of work rather than an optimisation pass. | dev | When the Social surface is measured on the real network path (PERFORMANCE_BASELINE §7) and the cost is felt. Then: fold the per-option reads into grouped aggregates, with the existing tabulation tests unchanged as the proof.  Since ADR-026 each of those reads also carries an `EXISTS` for the current campaign, so the route is now ~85 round trips and ~230 ms locally: the correct denominator costs more than the wrong one, and this is the route where consolidation would pay for itself first. **Traced 6 September 2026** (`docs/PERFORMANCE_BASELINE.md` §12) and left open: the trace found a duplicated campaign-scope predicate in four tally queries — a defect, removed, worth about 4 % and no change in round trips — and confirmed that the remaining cost is the per-question loop this entry is about. Resolving the current campaign once per render instead of four times would remove three round trips of eighty-three, at the price of passing a campaign id between read models that each open their own transaction: a wider change than the route deserves. |
| TD-066 | Nothing caches capability and membership rows for the life of a request. The two duplicate reads a render actually made are gone — the user row was reconciled twice and the shell re-read the tenant's capability rows — but they were removed by *returning* what had already been read, not by caching, so a future surface that asks twice would read twice. | A cache on an authorization decision is the wrong kind of thing to add casually: stale capabilities mean a decision made on old data. Request-scoped caching is safe; anything wider is not, and the distinction has to be enforced rather than intended. | dev | When a surface needs the same rows a third time and passing them through stops being natural. Never a cache that outlives one request. |
| ~~TD-067~~ | ~~The database pool sets neither `connectionTimeoutMillis` nor a `statement_timeout`.~~ **Resolved in the demo-readiness wave.** The pool now sets a connect timeout (10 s), a statement timeout (20 s, `null` for operator work), TCP keepalive, a bounded connection lifetime and a shorter idle timeout — chosen from the one observed `ECONNRESET` rather than from a checklist (`docs/PERFORMANCE_BASELINE.md` §11.1). No retries were added: a retry around a transaction re-runs writes. | — | — | Closed 2026-09-05. |
| TD-068 | The FieldFlow technician e2e specs consume a pending assignment each run and cannot restore one, so a **local** database eventually runs out and `field-technician.spec.ts` fails at "no assignment to start". CI is unaffected: it starts from a fresh container every time. The local remedy is `pnpm db:reset:local`. | A submitted response is immutable by design (invariant 9), so unlike the Quality Gate — where a change of mind is a legitimate second decision the suite can make through the product — there is no honest product path back to a pending assignment. The alternatives are a fixture with an unbounded pool of assignments, which hides the exhaustion rather than fixing it, or having the coordinator project create one before the technician project runs, which is real but restructures the field suite across two Playwright projects. | dev | The next change to the field e2e suite. Then: a coordinator-project dependency that assigns fresh work through `field.assignments.manage`, which is the product's own path and makes the suite re-runnable for the same reason the Quality Gate's is. |
| TD-073 | A project can hold more than one `ACTIVE` campaign: nothing in the schema prevents it, and `resolveCurrentCampaign` therefore has a tie-break (most recently activated) rather than a guarantee. | The pilot has one active campaign and the tie-break is deterministic, so the surfaces are correct today. A partial unique index would make it structural — and would also have to answer what a *second* concurrent operation means for a project that legitimately runs two crews on two corridors, which is a product question nobody has asked yet. | dev | The first project that wants two operations at once, or the first time the tie-break decides something a person disagrees with. Then: either a partial unique index on `(tenant_id, project_id) where status = 'ACTIVE'`, or an explicit "current" flag with its own transition. |
| TD-069 | The package's own `AREA` column is not stored, so the product cannot report that it disagrees with the geometry — and it does: the column totals 138 ha across the 141 parcels while PostGIS measures 550 ha from the same polygons. | Two numbers for one fact drift, and only one of them is reproducible: the product shows the computed area because a reader can re-derive it. Storing the declared value as well would turn a silent divergence into a Quality Gate rule, which is better — but it is a column, a rule and a decision about which figure a report quotes, not an importer's aside. | dev | The next Quality Gate slice. Then: a nullable `declared_area_m2` on `parcel_geometry` and a rule that states both numbers without saying which is right. |
| ~~TD-070~~ | ~~The four influence areas are stored and legended but not drawn, and the AISI polygon is 570 KB of GeoJSON for one feature.~~ **Resolved in the demo-readiness wave.** All four are drawn, under the parcels, with a toggle beside the legend that names them and their areas. The outline is generalised for display with `ST_SimplifyPreserveTopology` in the dataset's analysis CRS — 890 KB of stored coordinates become 21 KB drawn — and the surface says the drawn contour is generalised while the stored geometry is the study's. | — | — | Closed 2026-09-05. |
| ~~TD-071~~ | ~~The demo campaign's 12 assignments target the first 12 parcels by chainage, so the simulated field work clusters at one end of the corridor.~~ **Resolved in the real-data wave.** The seeder now takes every *total ÷ count*-th parcel in chainage order, deterministically: the twelve run from abscissa 0 to 6 035 m of a 7 361 m alignment. | — | — | Closed 2026-09-04. |
| ~~TD-072~~ | ~~The PGAS and the cartography are not cross-checked.~~ **Resolved in the demo-readiness wave.** `rule.pgas_place_vs_influence_area@1` compares a plan's *lugar de aplicación* against the areas of influence the cartography holds — a document against a geometry, two sources, no compliance claim. It is silent against the study as delivered (`PRC-01` names the AID and the AID exists), which is why the rule catalogue on the surface lists what was checked whether or not it fired; the detector's firing case is covered in `packages/domain/test/quality.test.ts`. The importer now also reads the *lugar de aplicación* out of the objective paragraph, where seven of the nine plans put it. | — | — | Closed 2026-09-05. |
| TD-074 | The PGAS chapter is imported as structure but not ingested as a `SourceDocument`, so a measure cannot yet be cited in a generated report and the chapter is not searchable by the assistant. | Wave C's scope was to model what the document contains. Ingesting the same file twice — once as structure, once as text — needs a decision about which representation a citation points at, and answering that badly would give the report generator two sources for one sentence. | dev | The environmental-management chapter of the report generator (TD-062's second `generated_report.kind`). Then a measure's `measure_code` becomes the citation anchor and the text chunk carries the quote. |
| TD-075 | The report chapter's narrative records the model and the prompt version but **not the tokens it used**, and the document assistant records nothing at all because nothing it produces is persisted (ADR-021). Assisted coding records model, provider, tokens and latency per classification. | Both are one call per human action over a bounded input — a snapshot of a few hundred words, or a handful of retrieved passages — so neither can run away with a budget the way a per-answer run could. Adding a column that would hold nothing until a provider is configured, in a wave whose whole premise is that none is, would be a migration written on speculation. | dev | The first live generation on a paid provider. Then: `input_tokens`/`output_tokens` on `report_version` beside `narrative_model`, and a decision about whether an assistant answer becomes recordable at all — which is really the question of whether it stays unpersisted. |
| TD-076 | The map viewer has **no automated proof that what it draws matches the payload** — only that the camera frames the project and that some polygons are rendered inside it. A layer that silently failed to paint, or painted the wrong colour, would still pass. | A canvas has no state a test can read, and the honest alternatives are both expensive: pixel comparison, which fails on font rendering and teaches a team to ignore it, or a headless WebGL readback, which asserts colours rather than meaning. The counts the map now publishes close the failure that actually happened — an empty viewport — without pretending to be a visual regression suite. | dev | The next time a map defect reaches a reviewer that the counts would not have caught. Then: decide between a bounded set of golden images for the two GIS surfaces and a `queryRenderedFeatures` assertion per layer, and write down why. |
| TD-077 | A publication has no *unpublish*. Withdrawing one (ADR-009 §3, "publication can be withdrawn immediately") is not implemented: the row is immutable and there is no state beside it saying it no longer applies. | Nothing external consumes a publication yet, so there is nothing to withdraw it from; and withdrawal is a second concept — a row that says *as of this instant, v3 is no longer what the client sees* — rather than a delete, which the immutability rules out on purpose. Adding a nullable `withdrawn_at` to an append-only table would be the editable field this design exists to avoid. | dev | External client access (TD-005). Then: a `client_publication_withdrawal` row in the shape `specialist_review` established — append-only, attributed, justified — and a client view that resolves the newest publication that has not been withdrawn. |
| TD-078 | The standalone client view is protected by the **internal** staging session, not by a client identity, and it is reachable only inside the protected Preview deployment. | External client authentication is TD-005; this entry records the consequence, which is that today's `/portal/:tenant/:project` is a *demonstration of what a client would see*, not a portal a client can open. The strip above the content says exactly that, in Spanish, on every render. | owner → dev | Together with TD-005. Until then, the route must never be exposed on a public hostname and no share token may be minted for it. |
| TD-079 | The publication builder reads figures the Command Center curated (`metric_snapshot`), so a figure the client should see must first exist as a Command Center metric. There is no publication-specific source of aggregates. | Those rows already carry a provenance record with a regime, which is what the publishable/not-publishable decision needs, and inventing a second aggregate store for the portal would give the two surfaces two versions of one number (IG1-002's warning, from the other direction). | dev | The first figure a client needs that a coordinator does not. Then: either curate it into the metric vocabulary deliberately, as that invariant intends, or give the builder a second typed reader — and say in the ADR which. |
| ~~TD-080~~ | ~~EIA Field ships no media capture.~~ **Resolved in Production V1 Wave 2 (ADR-032).** The local `local_media` table this entry described did not in fact exist and was added with the feature. The device copies a photograph into its own directory and writes the row *before* anything is attempted, uploads when there is signal, declares through the ordinary outbox, and deletes the local file only once the server acknowledges the row — the one irreversible step, behind one predicate. What is still missing is a bucket in a persistent environment (TD-090) and a capture on real hardware (TD-096). | — | — | Closed 2026-09-17. |
| TD-081 | **Background synchronisation is not implemented.** The guaranteed pathways are app launch, connectivity returning while the application is open, and the explicit button. | It is an enhancement, never the correctness mechanism (ADR-028): iOS gives no scheduling guarantee, a killed application runs nothing, and a design where data safety depended on a background task would be a design that loses data on the one phone whose owner force-quits. | dev | After the foreground pipeline has run a real campaign. Then `expo-background-task`, treated as best effort, with the same outbox and no new correctness claim. |
| TD-082 | **No native build artefact exists.** Wave 1 verified the JavaScript bundle for both platforms (`expo export`) and generated no `.apk` or `.ipa`. | The build machine has neither Xcode (command line tools only) nor the Android SDK nor a JDK, and distribution needs Apple and Google developer accounts — a paid action the brief's hard stops reserve for the owner. | owner → dev | A machine with the SDKs, or EAS Build. Then `pnpm --filter @eia/field prebuild` and a development build per platform, followed by `docs/FIELD_MOBILE_OFFLINE_UAT.md` on a handset. |
| TD-083 | The pull returns the technician's **whole** assignment set rather than an incremental diff, and the cursor records only when the device last synced. | The set is bounded by design — one technician, one campaign, twelve assignments in this pilot — and the field tables carry no `updated_at`, so a timestamp diff would have to be derived from workflow timestamps and would silently miss a reassignment: the change that matters most. A complete answer is self-correcting; a diff adds the one failure an offline client cannot recover from. | dev | A campaign where one technician holds enough assignments for the payload to be felt. Then `updated_at` with triggers on the field tables, and the cursor starts encoding a watermark — the protocol already treats it as opaque, so no version bump. |
| ~~TD-084~~ | ~~A submitted response cannot be corrected from the device.~~ **Resolved by ADR-038.** Not by reopening one — `SYNCED` is still terminal and the device still offers no edit — but by the technician receiving a **revisit**, which is an ordinary assignment and needs no new command. The entry asked for one workflow in the shape `specialist_review` established: append-only, attributed, justified. | — | — | Closed 18 Sep 2026. |
| TD-085 | A **client publication has no language**. Its payload is composed in Spanish and its page is rendered in Spanish, whatever the reader's interface is set to. An English-reading reviewer opening `/portal/:tenant/:project` sees the whole client view in Spanish. | A publication is a statement the firm made to its customer on a date, stored exactly as it was composed (ADR-027). Translating the headings while the published figures stay in the language they were published in would produce a page that is half one language and half the other, and would imply the firm said something it did not. Rendering the page in the publication's own language is the coherent half-measure; the honest full answer is a column. | dev | The first client who reads in English. Then: a `locale` on `portal.client_publication`, a publish action that says which language it is publishing in, and a client view that renders that language — with the older rows answering `es-EC`, which is what they are. |
| TD-086 | A **generated finding's stored text is in the language the check ran in**, which is Spanish. `title`, `explanation`, `why_flagged` and `suggested_action` are written to `quality_finding` when a run produces it, and the English surface shows them as stored. The *rule* behind a finding has words in both languages and the "Reglas vigentes" panel uses them. | A finding is a record of what a check said at a moment, and a decision somebody took was taken about those words (ADR-020). Translating stored text at read time would show a reviewer a sentence nobody wrote; re-running to get English would raise new findings and lose the decisions attached to the old ones. | dev | When an English-reading reviewer settles findings routinely. Then: generate from the rule catalogue's keys and interpolated values rather than from a rendered sentence — the finding stores the key and its arguments, and the surface composes the words — which is also what would let a rule's copy be corrected without re-running. |
| TD-087 | The sync protocol's free-text `message` on a rejected or conflicted command is **server-composed Spanish**, and EIA Field shows it when it has no word of its own. Every *outcome* the phone renders itself is a code (`conflictReason`, `localSurveyState`) and is bilingual; the detail beside it is not. | The codes cover every case the product designed for, and they are what a technician acts on. The remaining text is the bounded operational detail for a case nobody anticipated — precisely the case where inventing a catalogue key would be guessing. Making it a code would mean enumerating failures that have not happened. | dev | When a real campaign produces a detail message an English-reading technician has to act on. Then: a code plus arguments on the wire, with the free text kept as an operator-facing field the phone does not render. |
| ~~TD-088~~ | ~~Questionnaire and template authoring is not in the product.~~ **Resolved in two halves.** Report templates: ADR-036 (uploaded, validated and activated under *Informes*). Questionnaires: **ADR-037** — the intake's *Formularios* stage is where a `SurveyVersion` now comes from, behind `field.instruments.author` and `field.instruments.publish`, both read by that stage. The entry predicted `surveys.author` as one key; it shipped as two, because writing the instrument and deciding households will be asked it are different acts. | — | — | Closed 17 Sep 2026 (`docs/SURVEY_AUTHORING.md`). |
| ~~TD-089~~ | ~~The readiness set has no object-storage rule.~~ **Resolved in Production V1 Wave 2.** `project.storage` is the eighth rule: advisory, not applicable when neither `core.documents` nor `field.surveys` is enabled, and carrying the availability *reason* as a code the catalogue has words for — never the operator-facing detail, which names environment variables. It reads what `resolveStorageAvailability` decided, passed in by the caller rather than read in the application layer (ARCHITECTURE §9.1). | — | — | Closed 2026-09-17. |
| TD-090 | **No bucket exists in staging or production.** Both resolve `NOT_CONFIGURED`, the upload panel says so, and nothing else changes. | Creating one means an account, a paid subscription and a credential — an owner action the wave's hard stops reserve, and one ADR-012's portability constraint deliberately leaves open (AWS S3, Cloudflare R2, Backblaze B2, self-hosted MinIO all work). Provisioning it automatically would spend somebody's money and pick their vendor. | owner → dev | `docs/OBJECT_STORAGE.md` §7 is the four-step activation. No migration, no code change, no different build: five environment variables and the panel starts working. |
| TD-091 | **Nothing scans an uploaded file for malicious content.** The gate is an allowlist of two formats per namespace, checked by extension, declared MIME type and file signature, plus archive limits on the ZIP a `.docx` is. | That is a gate and does not pretend to be antivirus: a denylist would have to enumerate what is dangerous, which is the set nobody can finish, and a scanner is an external service with its own vendor, cost and data-residency question — which is the compliance gate's territory (SECURITY.md §10a), not an engineering aside. Nothing executes an uploaded file: extraction reads entries and never runs them. | owner → dev | A tenant whose policy requires scanning, or the first upload path open to somebody outside the consultancy. Then a scanner behind a port, run as a job between `UPLOADED` and `QUEUED`, with an `INFECTED` terminal state beside `FAILED`. |
| TD-092 | When the resolved provider is the in-memory store, the browser sends the file's bytes **through a server action** (`putLocalBytesAction`) instead of PUTting them to a URL, because that store's "presigned" URL is a token only this process understands. | Without it the upload surface could not be exercised locally or in the e2e suite, which would leave the one path a person actually takes untested. The action refuses unless this deployment genuinely resolved to `memory` — which `resolveStorageAvailability` permits only in `local` and `test` — and it grants nothing: `finalizeUpload` still reads the bytes back, checks the size and the signature, and computes the hash itself. | dev | If the e2e suite ever runs against MinIO. Then the branch and the action both go, and the browser PUTs in every environment. |
| ~~TD-093~~ | ~~A stored file cannot be downloaded from the surface.~~ **Resolved in Wave 3 (ADR-034).** A route on the version page mints a five-minute link after re-checking `documents.read` under the caller's own RLS, and audits the issuance as `document.version.download_issued` — with the document, the version and the privacy claim, and never the filename, the key or the hash. A version with no file, one of another project and one that does not exist all answer 404. | — | — | Closed 2026-09-17. |
| ~~TD-094~~ | ~~`content_hash` holds the *file* hash while a version is unprocessed.~~ **Resolved in Production V1 Wave 2 (ADR-033 §7).** Extraction writes the hash of the extracted text, so a `READY` version's two hashes differ; the integration suite asserts it. An unprocessed version still carries the file hash there, which is the honest stand-in the entry described. | — | — | Closed 2026-09-17. |
| TD-095 | A retried **upload** may mint a fresh intent, and therefore a fresh key and a fresh object, leaving the earlier object with nothing pointing at it. Nothing sweeps those. | The alternative — reusing an intent whose PUT may have half-succeeded — risks the photograph rather than the bucket, and that is the wrong thing to risk. An object nobody points at is housekeeping; a lost photograph is evidence that no longer exists. The volume is bounded by how often a PUT fails after the provider stored bytes, which is rare. | dev | The first bucket with a real cost, or the first storage bill anybody reads. Then: a job that lists `upload_intent` rows in `ISSUED` past their expiry and removes the objects under their keys — safe precisely because a finalized intent's object is never one of them. |
| TD-096 | **No photograph has been taken on real hardware.** The camera path is proved by unit tests over an injected transport, by an integration suite against real MinIO and PostgreSQL, and by a bundle that exports for Android and iOS — and not by a handset. | There is still no native build artefact (TD-082): the build machine has neither Xcode nor the Android SDK, and distribution needs paid developer accounts, which is an owner action. `expo-image-picker`'s camera cannot run in a bundle nobody installed. | owner → dev | Together with TD-082. Then `docs/FIELD_MOBILE_OFFLINE_UAT.md` §7 on a real device: take three photographs in aeroplane mode, force-quit, restore connectivity, and confirm one row per photograph and the local files gone only afterwards. |
| TD-097 | A photograph cannot be **removed** once declared, by anybody, including the technician who took it by mistake. | `field_media` is written once for the reason every append-only table here is: what a photograph is of, and when, are statements somebody made, and a row that could be quietly rewritten would leave evidence attributed to a person who did not produce it. Removal is a second concept — who may, on what basis, what happens to the object — and this product has no such workflow for a report version (TD-060) or a submitted response (TD-084) either. | dev | The first real request from a campaign. Then one workflow for all three, in the shape `specialist_review` established: append-only, attributed, justified — never a delete. |
| TD-098 | **OCR is not built.** A scanned PDF is `REQUIRES_OCR`: the file stays available, the surface says why with the numbers, and no chunk is written — so it can never be cited. What it cannot do is become searchable. | OCR means either an external service or a large local model. Both are a cost and a dependency; the first is also a **processor** handling a study's documents, which may contain personal data, and SECURITY.md §10a's compliance review has not assessed any such vendor. Shipping a mediocre local OCR would be worse than the honest state: its output is indistinguishable from real text once it is a chunk, and a citation would then point at words nobody wrote (the artefact IG4-001 exists to prevent, one layer over). | owner (vendor decision) → dev | A consultancy whose corpus is substantially scanned. Then: an `OcrPort` behind the same explicit-configuration rule as the classifier, a `DocumentTextSource` value of its own so an OCRed passage is never mistaken for native text, and the vendor in the processor registry first. |
| TD-099 | Extraction's **memory profile is unmeasured**. A 2 000-page PDF's text is held in one array before chunking, and a DOCX's body XML is decoded into one string. Bounded by `PDF_LIMITS` and `ARCHIVE_LIMITS`, but bounded is not measured. | The bounds were chosen to be safely above any real study and safely below anything that could exhaust a worker, and the pilot's documents are two orders of magnitude smaller. Measuring properly needs a real corpus of eight studies, which arrives with the projects. | dev | The first study whose files are large enough for the worker to be felt, or the first `WORKER_MEMORY` alarm. Then: stream pages into the chunker instead of collecting them, which the chunker's interface already allows. |
| ~~TD-100~~ | ~~The worker and the web application each resolve their own storage adapter, so `memory` is two stores.~~ **Resolved in Wave 3 (ADR-034 §6).** `pnpm e2e` starts one MinIO that both Playwright web servers use, and `e2e/document-pipeline.spec.ts` follows a file from a browser upload through that store to a separate worker process and back to a citation on screen. The per-process in-memory store remains the fallback when the container is absent, and the spec skips with the reason rather than passing on a topology nobody has. | — | — | Closed 2026-09-17. |
| TD-101 | A presigned download URL **contains the object key**, because that is what signing a fetch of an object means. A link pasted into a chat therefore discloses the tenant id, the project id and the object id. | It discloses nothing about *whose* document it is: ADR-031's key is a namespace and four UUIDs — no filename, no code, no date, no person. The alternative is streaming the bytes through the application, which is the cost presigned URLs exist to avoid (a 120 MB study through a serverless function), and SECURITY.md §7's actual rule — the UI never receives raw keys of other objects — is met, because the page holds a route. | owner (policy) → dev | A tenant whose policy forbids the object path being visible at all. Then: a streaming route with a bounded size, and the presigned path kept for anything above it. |
| TD-102 | The e2e pipeline spec runs **one drain of the worker's queue in-line**, not the worker's own loop: no polling, no backoff, no stale-release. | Those three are covered by the integration suite against real MinIO, and starting a worker process, waiting for it to poll and stopping it adds three places for a flake that says nothing about the product. What the pipeline test exists to prove is that bytes one process wrote are bytes another can read. | dev | When the loop itself is suspected — a claim that never returns, a backoff that never ends. Then: run `apps/worker` as a real process in the suite and wait on the row rather than on the drain. |
| TD-103 | An AI review's **lenses and their retrieval probes are code, not configuration**: a consultancy cannot add a lens, and the probes are Spanish terms tuned against one corpus. | The same reasoning ADR-020 gave for the rule catalogue: a lens that a tenant could edit would be a prompt a tenant could edit, and the bounded vocabulary is the control that keeps "review this study" from becoming an unbounded question. Probes in a table would also need a review workflow nobody has asked for. | dev | The second or third project whose corpus is worded differently enough that a lens retrieves nothing. Then: probes as a versioned catalogue entry beside the lens, with the lens ref already stored on every run so old candidates stay attributable. |
| TD-104 | A review run's corpus is the **current version of each selected document**, and a superseded version can never be reviewed. | A review says something about the study as it now stands; candidates about words a corrected delivery already replaced would be work a specialist has to discard. The superseded version stays readable so old citations resolve (ADR-031), which is the property that actually matters. | dev | A request to compare a delivery against its predecessor — which is a different feature (a diff), not a wider corpus. |
| TD-105 | An **accepted candidate goes nowhere**: it is a specialist's note on the review page, and no report, deliverable or client publication reads it. | A report snapshot admits five typed sources (ADR-022) and a model's suggestion that a person agreed with is not one of them — adding it would need a sixth regime, a provenance shape and a decision about how the prose may phrase it, none of which this wave has thought through. Preserving the acceptance honestly is worth more than wiring it somewhere prematurely. | dev | The first consultancy that asks for accepted candidates in the deliverable's review annex. Then: a sixth source kind whose rendering names the specialist who accepted it, never the model. |
| TD-106 | The **deterministic reviewer is thin**: it pairs the first two passages and describes them, so the suite exercises the plumbing, the refusals and the decisions rather than the quality of candidates. | Nothing can test the quality of a model's suggestions in CI, and a richer fake would only be testing itself. What the suite must prove is what happens to a suggestion — grounding, vocabulary, the privacy gate, the two-sided rule, the decision record — and that it does. | dev | When a live provider is authorised. Then: a recorded-response fixture from a real run, kept beside the fake rather than replacing it. |
| TD-107 | The template renderer depends on `easy-template-x`, which **pins** `@xmldom/xmldom@0.8.13`; the advisory-clean `0.8.15` is forced by a workspace `pnpm.overrides`. | 0.8.13 carries ten open advisories, several of them quadratic-time or quadratic-memory parsing reachable from an uploaded template — exactly the untrusted input this feature accepts. 0.8.15 is a patch inside the same LTS line, the API is unchanged, and the suite exercises it against real Word packages. The alternative was adopting a renderer with known-vulnerable parsing or writing our own. | dev | When `easy-template-x` relaxes or raises the pin. Then: drop the override and re-run `pnpm audit`. |
| TD-108 | A template can **hide** the mandatory draft banner — white text, 2pt, behind an image. | The product guarantees it never produces a document without the banner having rendered, which is what it can guarantee. Laying out somebody else's deliverable to place the banner ourselves would mean editing their Word XML, and a firm determined to remove it would equally delete an injected paragraph. | owner (policy) | A client engagement where the banner's prominence is contractual. Then: a rendered-PDF check, or a product-drawn header part rather than a placeholder. |
| TD-109 | A generated document can name a `ReportVersion` and its snapshot digest, and **nothing renders the chapter's prose through a template**: the two generators are side by side. | The chapter generator (ADR-022) produces its own `.docx` from the snapshot and is complete; the template library fills a firm's format from project-level values. Joining them means a placeholder vocabulary over *sections*, which is a design decision about the deliverable's shape rather than a wiring task. | dev | The first consultancy that wants the social chapter inside their own chapter template. Then: section-scoped placeholders, with the snapshot still the thing that is validated first. |
| TD-110 | **No real consultancy template has been received.** Every template the suite exercises is synthetic and named as such. | Real templates are the firm's own commercial artefacts and arrive with the engagement; building against invented ones risks a vocabulary shaped by our imagination rather than their documents. The registry is deliberately small so that meeting a real template adds keys rather than reshaping the model. | owner | The first delivered template. Then: read it, list the placeholders it would need, and extend the registry only where a deterministic or validated source exists. |
| TD-111 | A tenant **ADMIN can create a project and cannot then prepare it**: creating is a tenant permission, preparing is a project one, and an ADMIN has no implicit project access (TENANCY.md §2.1). There is no members surface to add themselves with. | An OWNER has implicit, computed, audited access to every project of the tenant, so the path works today for the role a consultancy's principal actually holds. Minting an implicit membership for an ADMIN would quietly widen D-015's deliberate split between administering a tenant and reading a project's data — the opposite of what that decision is for. | dev | The first firm whose project setup is done by an administrator rather than the owner. Then: a project-members surface behind `project.members.manage`, which is the permission an ADMIN already holds. |
| TD-112 | `ROAD_EIA_SOCIAL_PROFILE.label` and `.description` are Spanish strings in `@eia/domain`, which ADR-029 moved copy out of. | They reach no screen: the Portfolio renders `vocabulary.profile.road_eia_social` from the catalogue, and `profileLabel()` has no caller. Removing them means deciding whether a profile's description belongs in the catalogue (it is product copy) or in the profile (it is part of what a profile *is*), which is a small design question nobody has needed answered. | dev | The second profile, which is when the question becomes real. Then: profile copy in the catalogue keyed by profile key, as the Portfolio already does. |
| TD-113 | **Staging has no object storage bucket**, so the upload, media, extraction, download and template paths cannot be exercised there — only their `NOT_CONFIGURED` state can. | Creating a bucket is an owner action with a cost (TD-090); the product's own rule is that unset storage is a reported state rather than a fallback (ADR-031), and staging reports it correctly. Everything that does not need bytes is exercised there: migrations 0034…0047 are applied and `pnpm test:staging` passes 99 assertions against the real database. | owner | `docs/STAGING_OPERATIONS.md` §3's four steps. Then: the document and media smoke tests of §4. |
| TD-115 | **A correction is never reassigned.** It goes to whoever held the original assignment, and there is no surface for giving it to a different technician — the use-case accepts an assignee but nothing calls it with one. | The ordinary case is the same technician returning to the same parcel, and the decision *who should re-do this* is a coordinator's judgement that belongs beside the other assignment actions rather than buried in a correction form. Shipping a picker nobody asked for would be guessing at that workflow. | dev | The first correction a coordinator wants somebody else to capture. Then: reassignment on the correction's own assignment, through the assignment surface that already exists for ordinary work. |
| TD-116 | **A cancelled correction leaves a `CANCELLED` assignment in the campaign.** It is excluded from progress and from the Field Pack, and it is never deleted. | Deleting it would destroy the record that somebody was asked to revisit a parcel and then told not to, which is exactly the history ADR-038 exists to keep. A cancelled ordinary assignment behaves the same way and always has. | dev | If a campaign ever accumulates enough cancelled corrections for the count to mislead. Then: a separate cancelled-corrections figure, not a delete. |
| TD-117 | **`survey_instance.respondent_user_id` holds the technician's user id, not a respondent's.** It is the caller who captured the response, set from the session in `ensureInstance`. | Found while writing the privacy checklist, and it is a naming defect rather than a behavioural one: no code reads it as a household member, there is no respondent entity, and every policy and read model treats it as the capturer. Renaming a column that every RLS policy, read model and index touches is a migration with no behavioural benefit, and doing it in a wave about go-live readiness would be a schema change nobody asked for. | dev | The wave that introduces a real respondent entity — which is when the name becomes actively wrong rather than merely misleading. Then: rename to `captured_by_user_id` and let the new entity take the name. Documented meanwhile in `docs/PRODUCTION_PRIVACY_CHECKLIST.md` §2.1. |
| TD-118 | **The Social Intelligence e2e specs fail intermittently under a loaded full-suite run**, with a server error on the page rather than an assertion failure. Seen twice in one 9.8-minute run on 18 Sep 2026; the same project passes 27/27 in isolation, twice, and the same suite passed 268/268 twice earlier the same day on the same commit. | Investigated rather than assumed: the most plausible code cause was the effective-response `EXISTS` that ADR-038 added to every Social query, so it was measured — **0.379 ms against 0.065 ms** on the pilot's data, with planning 2.1 ms against 0.6 ms. Against a 20-second statement timeout that is not the cause. The remaining explanation is contention: two Next servers, PostgreSQL, MinIO and Chrome on one machine. Social already issues 78 queries per render (TD-065), so it is the page most exposed to it. | dev | Either it recurs in CI — where the machine is different — or TD-065's query count is reduced. Then: capture the server-side error rather than the page snapshot, which is what would have identified this in one run instead of three. |
| ~~TD-114~~ | ~~A submitted response cannot be corrected.~~ **Resolved by ADR-038**, in the shape this entry predicted: a correction request, a new `SurveyInstance` that explicitly supersedes the prior one, and one deterministic effective-instance resolver — `app.effective_survey_instance` — reused by every reader. | — | — | Closed 18 Sep 2026. |
